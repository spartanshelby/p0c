subfinder -d domain.com | httpx -mc 200 | tee subdomains.txt && cat subdomains.txt | waybackurls | httpx -mc 200 | grep .js$ | tee js.txt

nuclei -l js.txt -t ~/nuclei-templates/exposures/ -o js_nuclei_results.txt

grep -E "ikea" url.txt > ikea.txt
remove .js from file
grep -v '\.js$' file.txt 

another way 

code 
file="js.txt"

# Loop through each line in the file
while IFS= read -r link
do
    # Download the JavaScript file using wget
    wget "$link"
done < "$file"



grep -r -E "aws_access_key|aws_secret_key|api key|passwd|pwd|heroku|slack|firebase|swagger|aws_secret_key|aws key|password|ftp password|jdbc|db|sql|secret jet|config|admin|pwd|json|gcp|htaccess|.env|ssh key|.git|access key|secret token|oauth_token|oauth_token_secret|smtp|xmlrpc|firebase" *.js

grep -r -E "aws_access_key|aws_secret_key|api key|passwd|pwd|heroku|slack|firebase|swagger|aws_secret_key|aws key|password|ftp password|jdbc|db|sql|secret jet|config|admin|pwd|json|gcp|htaccess|.env|ssh key|.git|access key|secret token|oauth_token|oauth_token_secret|smtp|xmlrpc|firebase" *.js | awk '{print $1,$0}'

cat js.txt | grep -r -E"aws_access_key|aws_secret_key|api_key|passwd|pwd|heroku|slack|firebase|swagger|aws_secret_key|aws_key|password|ftp_password|jdbc|db|sql|secret|jet|config|admin|pwd|json|gcp|htaccess|.env|ssh_key|.git|access_key|secret_token|oauth_token|oauth_token_secret"



cat js.txt | while read url; do python3 ~/Desktop/secretfinder/SecretFinder.py -i $url -o cli >> secretjsss.txt; done 



cat ~/Desktop/namshi/js_files.txt | xargs -I{} curl -s {} | ./extract.rb



cat test.txt | while read url; do python3 SecretFinder.py -i $url -o cli >> secret.txt; done

